Index: src/algo.c
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/algo.c b/src/algo.c
--- a/src/algo.c	
+++ b/src/algo.c	(date 1682149981000)
@@ -23,25 +23,6 @@
 	return (bit_max);
 }
 
-int	check_neg(t_list *stack_a)
-{
-	int	size;
-	int	length;
-
-	size = ft_lstsize(stack_a);
-	length = 0;
-	while (stack_a)
-	{
-		if (stack_a->data <= 0)
-			break ;
-		stack_a = stack_a->next;
-		length++;
-	}
-	if (length < size)
-		return (1);
-	return (0);
-}
-
 void	new_list_second_part(t_list *current, int min)
 {
 	while (current != NULL)
